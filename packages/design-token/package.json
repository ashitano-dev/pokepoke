{
  "name": "@pokepoke/design-token",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "gen:colors": "tsx ./src/colors/export-radix-colors/script.ts",
    "check": "biome check --error-on-warnings --no-errors-on-unmatched",
    "check:fix": "pnpm check --write",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@radix-ui/colors": "^3.0.0"
  },
  "devDependencies": {
    "@pokepoke/tsconfig": "workspace:*"
  }
}
