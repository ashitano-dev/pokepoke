{
  "name": "@pokepoke/eden-fetcher",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "biome check --error-on-warnings --no-errors-on-unmatched",
    "check:fix": "pnpm run check --write"
  },
  "dependencies": {
    "@elysiajs/eden": "^1.2.0"
  },
  "devDependencies": {
    "@pokepoke/backend": "workspace:*",
    "@pokepoke/core": "workspace:*",
    "@pokepoke/tsconfig": "workspace:*"
  }
}
